import Container from '@/ui/layout/container'
import React from 'react'

type Props = {}

const ResumeBuilder = (props: Props) => {
  return (
    <Container className=''>Resume Builder</Container>
  )
}

export default ResumeBuilder